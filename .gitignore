# Dependencies and build output
node_modules
**/node_modules
dist
dist-ssr
.output
.nitro
.tanstack
.vinxi
.wrangler
__unconfig*
spikes/**/dist
*.local
.DS_Store
todos.json

# Secrets: never commit .env (the example is committed)
.env
.env.*
!.env.example

# Local data (e-mail outbox, caches), agent workspaces (SPEC §5.6, §5.7)
.data/
.cache/
.workspace/

# e2e output and saved logins
e2e/.auth/
**/test-results
**/playwright-report
e2e/shots

src/features/**/CONTRACT_REQUESTS.md.bak

# Test output
.vitest/

# Internal specs, QA notes and design docs: kept locally (private), not published
/docs/
/qa/
